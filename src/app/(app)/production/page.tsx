import { requireSession } from "@/lib/auth";
import { db } from "@/lib/db";
import { formatMilli,formatPKR,integerToBigInt,multiplyQuantityRate } from "@/lib/money";
import { karachiBusinessDate } from "@/lib/queries";
import { ProductionForm,ProductionPrintButton,ProductionReversal } from "./production-form";
export const dynamic="force-dynamic";
export default async function ProductionPage({searchParams}:{searchParams:Promise<{date?:string}>}){const session=await requireSession(),filters=await searchParams,database=await db(),today=karachiBusinessDate(),match:Record<string,unknown>={yogurtProductSku:"YOG-001"};if(filters.date)match.businessDate=filters.date;const[milk,yogurt,batches,stats]=await Promise.all([database.collection("products").findOne({sku:"MILK-001"}),database.collection("products").findOne({sku:"YOG-001"}),database.collection("production_batches").find(match).sort({businessDate:-1,createdAt:-1}).limit(100).toArray(),database.collection("production_batches").aggregate([{$match:{businessDate:today,status:"posted",yogurtProductSku:"YOG-001"}},{$group:{_id:null,milk:{$sum:"$milkUsedMilli"},output:{$sum:"$actualOutputMilli"},profit:{$sum:"$estimatedGrossProfitPaisa"},kundas:{$sum:{$sum:"$kundaEntries.numberOfKundas"}}}}]).next()]);const milkStock=integerToBigInt(milk?.stockMilli),yogurtStock=integerToBigInt(yogurt?.stockMilli),yogurtCost=integerToBigInt(yogurt?.averageCostPaisa),yogurtRate=integerToBigInt(yogurt?.retailRatePaisa),yieldValue=integerToBigInt(stats?.milk)>0n?integerToBigInt(stats?.output)*1000n/integerToBigInt(stats?.milk):0n;const cards=[["Fresh Milk available",`${formatMilli(milkStock)} L`],["Yogurt available",`${formatMilli(yogurtStock)} kg`],["Yogurt selling price",`${formatPKR(yogurtRate)} / kg`],["Yogurt average cost",`${formatPKR(yogurtCost)} / kg`],["Yogurt stock value",formatPKR(multiplyQuantityRate(yogurtStock,yogurtCost))],["Milk converted today",`${formatMilli(integerToBigInt(stats?.milk))} L`],["Yogurt produced today",`${formatMilli(integerToBigInt(stats?.output))} kg`],["Kundas prepared today",String(stats?.kundas??0)],["Production yield",`${formatMilli(yieldValue)} kg/L`],["Estimated gross margin",formatPKR(integerToBigInt(stats?.profit))]];return <div className="content"><div className="customer-heading"><div><div className="title">Yogurt / Kunda Production</div><div className="subtitle">Convert Fresh Milk into Yogurt and record 3 kg, 3.5 kg or custom Kundas.</div></div><ProductionForm today={today} milkStockMilli={milkStock.toString()} milkCostPaisa={integerToBigInt(milk?.averageCostPaisa).toString()} yogurtRatePaisa={yogurtRate.toString()}/></div><section className="grid kpis production-kpis">{cards.map(([label,value])=><article className="card" key={label}><div className="kpi-label">{label}</div><div className="kpi-value">{value}</div></article>)}</section><div className="card table-card"><div className="customer-heading"><div className="section-title">Yogurt production history</div><form className="toolbar"><input type="date" name="date" defaultValue={filters.date}/><button className="button secondary">Filter</button></form></div>{batches.length?<div className="table-scroll"><table className="table"><thead><tr><th>Batch</th><th>Date</th><th>Milk / cost</th><th>Kundas</th><th>Loose</th><th>Output</th><th>Cost/kg</th><th>Sell/kg</th><th>Yield</th><th>Est. profit</th><th>Status</th><th>Actions</th></tr></thead><tbody>{batches.map(batch=>{const entries=(batch.kundaEntries??[]) as Array<Record<string,unknown>>,count=(size:string)=>entries.filter(e=>String(e.sizeType)===size).reduce((sum,e)=>sum+Number(e.numberOfKundas??0),0);return <tr key={batch._id.toString()}><td><b>{String(batch.transactionNo)}</b></td><td>{String(batch.businessDate)}</td><td>{formatMilli(integerToBigInt(batch.milkUsedMilli))} L<div className="subtitle">{formatPKR(integerToBigInt(batch.milkMaterialCostPaisa))}</div></td><td>3 kg: {count("3")}<br/>3.5 kg: {count("3.5")}<br/>Custom: {count("custom")}</td><td>{formatMilli(integerToBigInt(batch.looseYogurtMilli))} kg</td><td>{formatMilli(integerToBigInt(batch.actualOutputMilli))} kg</td><td>{formatPKR(integerToBigInt(batch.yogurtUnitCostPaisa))}</td><td>{formatPKR(integerToBigInt(batch.yogurtSellingRatePaisa))}</td><td>{formatMilli(integerToBigInt(batch.yieldMilli))}</td><td>{formatPKR(integerToBigInt(batch.estimatedGrossProfitPaisa))}</td><td><span className="badge">{String(batch.status)}</span></td><td><div className="toolbar"><details><summary className="button secondary">View</summary><div className="receipt-popover"><b>{String(batch.transactionNo)}</b><p>{String(batch.notes??"No notes")}</p><p>Total cost: {formatPKR(integerToBigInt(batch.totalProductionCostPaisa))}</p></div></details><ProductionPrintButton/>{session.role==="owner"?<ProductionReversal transactionNo={String(batch.transactionNo)} status={String(batch.status)}/>:null}</div></td></tr>})}</tbody></table></div>:<div className="empty-state"><b>No Yogurt batches yet</b><span>Create a Yogurt Batch to convert Fresh Milk into sellable Yogurt.</span></div>}</div></div>}
