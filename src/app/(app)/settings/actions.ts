"use server";
import{Long}from"mongodb";import{revalidatePath}from"next/cache";import{z}from"zod";import{requireSession}from"@/lib/auth";import{db}from"@/lib/db";import{quantityToMilli,rupeesToPaisa}from"@/lib/money";
const schema=z.object({businessName:z.string().trim().min(2),address:z.string().trim(),phone:z.string().trim(),currency:z.literal("PKR"),timezone:z.string().trim().min(3),customerRate:z.string(),shopRate:z.string(),invoicePrefix:z.string().trim().min(1).max(8),closingTime:z.string(),allowedBackdateDays:z.coerce.number().int().min(0).max(31),yogurtAutomaticMilkRatioParts:z.coerce.number().int().min(1).max(1000),yogurtAutomaticOutputRatioParts:z.coerce.number().int().min(1).max(1000),yogurtYieldToleranceMilli:z.coerce.number().int().min(0).max(200),yogurtDefaultProductionMode:z.enum(["automatic","manual"]),milkInventoryUnit:z.enum(["liter","kilogram"]),milkDensityMilliKgPerLiter:z.string()});
export type SettingsState={error?:string;success?:string};
export async function saveSettings(_:SettingsState,data:FormData):Promise<SettingsState>{const actor=await requireSession(["owner"]),parsed=schema.safeParse(Object.fromEntries(data));if(!parsed.success)return{error:parsed.error.issues[0]?.message??"Check the settings."};try{const value=parsed.data;if(value.yogurtAutomaticOutputRatioParts>=value.yogurtAutomaticMilkRatioParts)return{error:"Yogurt output ratio must be less than the Milk ratio."};let density:bigint|null=null;if(value.milkInventoryUnit==="liter"){try{density=quantityToMilli(value.milkDensityMilliKgPerLiter)}catch{return{error:"Enter Milk density in kilograms per liter."}}if(density<=0n)return{error:"Milk density must be greater than zero."}}const database=await db(),now=new Date(),customerRatePaisa=Long.fromBigInt(rupeesToPaisa(value.customerRate)),shopRatePaisa=Long.fromBigInt(rupeesToPaisa(value.shopRate)),yieldMilli=Math.floor(value.yogurtAutomaticOutputRatioParts*1000/value.yogurtAutomaticMilkRatioParts),lossMilli=1000-yieldMilli;await database.collection("business_settings").updateOne({_id:"default" as never},{$set:{...value,customerRatePaisa,shopRatePaisa,yogurtAutomaticYieldMilli:yieldMilli,yogurtAutomaticLossMilli:lossMilli,yogurtMilkInputUnit:"kilogram",milkDensityMilliKgPerLiter:density?Long.fromBigInt(density):null,updatedAt:now,updatedBy:actor.userId},$setOnInsert:{createdAt:now,createdBy:actor.userId}},{upsert:true});await database.collection("audit_logs").insertOne({actorId:actor.userId,action:"update",entity:"business_settings",entityId:"default",metadata:{yogurtRatio:`${value.yogurtAutomaticMilkRatioParts}:${value.yogurtAutomaticOutputRatioParts}`,milkInventoryUnit:value.milkInventoryUnit},createdAt:now});for(const path of ["/settings","/production","/reports"])revalidatePath(path);return{success:"Business settings saved."}}catch{return{error:"Settings could not be saved. Check the database connection."}}}
