import {Sidebar} from "@/components/sidebar";import {Search,CalendarDays,Wifi} from "lucide-react";
export default function AppLayout({children}:{children:React.ReactNode}){return <div className="shell"><Sidebar/><main className="main"><header className="topbar"><div className="toolbar"><button className="button secondary"><Search size={14}/> Search</button><span className="subtitle"><Wifi size={13} style={{display:"inline"}}/> Connected</span></div><div className="toolbar"><button className="button secondary"><CalendarDays size={14}/> Today</button><div className="badge">Owner</div></div></header>{children}</main></div>}
