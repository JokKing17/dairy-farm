"use client";
import { useActionState, useEffect, useMemo, useRef, useState } from "react";
import { formatMilli, formatPKR, multiplyQuantityRate, quantityToMilli, rupeesToPaisa } from "@/lib/money";
import { addInventory, reverseReceipt, type InventoryState, type ReversalState } from "./actions";
type Product={sku:string;name:string;unit:string;stockMilli:string;averageCostPaisa:string;retailRatePaisa:string};
export function AddInventoryForm({products,today}:{products:Product[];today:string}){const[state,action,pending]=useActionState(addInventory,{} as InventoryState),[open,setOpen]=useState(false),[review,setReview]=useState(false),[key,setKey]=useState(()=>crypto.randomUUID()),handled=useRef<string|undefined>(undefined),[businessDate,setDate]=useState(today),[sku,setSku]=useState(products[0]?.sku??""),[quantity,setQuantity]=useState(""),[buyingPrice,setBuyingPrice]=useState(""),[sellingPrice,setSellingPrice]=useState(""),[keepRate,setKeepRate]=useState(false),[paymentStatus,setPaymentStatus]=useState<"paid"|"partial"|"unpaid">("paid"),[paymentMethod,setPaymentMethod]=useState("cash"),[paidAmount,setPaidAmount]=useState(""),[supplierName,setSupplierName]=useState(""),[supplierReference,setSupplierReference]=useState(""),[notes,setNotes]=useState("");const product=products.find(item=>item.sku===sku)??products[0];
  const calculation=useMemo(()=>{try{const received=quantityToMilli(quantity),buy=rupeesToPaisa(buyingPrice),oldStock=BigInt(product?.stockMilli??0),oldCost=BigInt(product?.averageCostPaisa??0),newStock=oldStock+received,newAverage=newStock>0n?(oldStock*oldCost+received*buy)/newStock:0n;return{received,total:multiplyQuantityRate(received,buy),oldStock,oldCost,newStock,newAverage}}catch{return null}},[quantity,buyingPrice,product]);
  useEffect(()=>{if(state.result&&handled.current!==state.result.transactionNo){handled.current=state.result.transactionNo;setKey(crypto.randomUUID());setReview(false);setQuantity("");setBuyingPrice("");setSellingPrice("");setPaidAmount("")}},[state.result]);
  const payload=JSON.stringify({businessDate,idempotencyKey:key,supplierName,supplierReference,paymentStatus,paymentMethod:paymentStatus==="unpaid"?undefined:paymentMethod,paidAmount:paymentStatus==="partial"?paidAmount:undefined,notes,lines:[{productSku:sku,quantity,buyingPrice,sellingPrice:keepRate?undefined:sellingPrice,keepExistingSellingPrice:keepRate}]});
  return <>{<button className="button inventory-primary" type="button" onClick={()=>setOpen(true)}>Add Inventory</button>}{open?<div className="review-dialog"><form action={action} className="card inventory-form"><input type="hidden" name="payload" value={payload}/><div className="customer-heading"><div><div className="section-title">Add Inventory</div><div className="subtitle">Receive Yogurt, Bread, Eggs or Ispaghol.</div></div><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Close</button></div>{state.error?<div className="form-error" role="alert">{state.error}</div>:null}{state.result?<div className="form-success"><b>Saved {state.result.transactionNo}</b><span>Purchase total {formatPKR(BigInt(state.result.subtotalPaisa))}</span></div>:null}<div className="formgrid"><div className="field"><label>Business date</label><input type="date" value={businessDate} onChange={event=>setDate(event.target.value)}/></div><div className="field"><label>Product</label><select value={sku} onChange={event=>{setSku(event.target.value);setSellingPrice("")}}>{products.map(item=><option value={item.sku} key={item.sku}>{item.name}</option>)}</select></div><div className="field"><label>Quantity received ({product?.unit})</label><input value={quantity} inputMode="decimal" onChange={event=>setQuantity(event.target.value)} autoFocus/></div><div className="field"><label>Buying price per {product?.unit}</label><input value={buyingPrice} inputMode="decimal" onChange={event=>setBuyingPrice(event.target.value)}/></div><div className="field"><label>Selling price per {product?.unit}</label><input value={sellingPrice} inputMode="decimal" disabled={keepRate} onChange={event=>setSellingPrice(event.target.value)} placeholder={formatPKR(BigInt(product?.retailRatePaisa??0)).replace("PKR ","")}/><label className="inline-check"><input type="checkbox" checked={keepRate} onChange={event=>setKeepRate(event.target.checked)}/> Keep existing selling price</label></div><div className="field"><label>Payment status</label><select value={paymentStatus} onChange={event=>setPaymentStatus(event.target.value as typeof paymentStatus)}><option value="paid">Paid</option><option value="partial">Partially paid</option><option value="unpaid">Unpaid / On credit</option></select></div>{paymentStatus!=="unpaid"?<div className="field"><label>Payment method</label><select value={paymentMethod} onChange={event=>setPaymentMethod(event.target.value)}><option value="cash">Cash</option><option value="bank">Bank</option><option value="easypaisa">Easypaisa</option><option value="jazzcash">JazzCash</option></select></div>:null}{paymentStatus==="partial"?<div className="field"><label>Amount paid</label><input value={paidAmount} inputMode="decimal" onChange={event=>setPaidAmount(event.target.value)}/></div>:null}<div className="field"><label>Supplier name (optional)</label><input value={supplierName} onChange={event=>setSupplierName(event.target.value)}/></div><div className="field"><label>Supplier invoice/reference (optional)</label><input value={supplierReference} onChange={event=>setSupplierReference(event.target.value)}/></div><div className="field"><label>Notes (optional)</label><textarea value={notes} onChange={event=>setNotes(event.target.value)}/></div><div className="field"><label>Receipt image</label><input disabled placeholder="Attachments are not configured"/></div></div>{calculation?<div className="inventory-preview"><span><small>Quantity received</small><b>{formatMilli(calculation.received)} {product?.unit}</b></span><span><small>Total purchase cost</small><b>{formatPKR(calculation.total)}</b></span><span><small>Existing stock</small><b>{formatMilli(calculation.oldStock)}</b></span><span><small>New stock</small><b>{formatMilli(calculation.newStock)}</b></span><span><small>Existing average cost</small><b>{formatPKR(calculation.oldCost)}</b></span><span><small>New average cost</small><b>{formatPKR(calculation.newAverage)}</b></span><span><small>Existing selling price</small><b>{formatPKR(BigInt(product?.retailRatePaisa??0))}</b></span><span><small>New selling price</small><b>{keepRate?formatPKR(BigInt(product?.retailRatePaisa??0)):sellingPrice?formatPKR(rupeesToPaisa(sellingPrice)):"—"}</b></span></div>:null}<button type="button" className="button" disabled={!calculation||pending||Boolean(state.result)} onClick={()=>setReview(true)}>Review Inventory Receipt</button>{review?<div className="review-dialog nested"><div className="card review-card"><div className="section-title">Confirm inventory receipt</div><div className="review-line"><span>{product?.name}</span><b>{quantity} {product?.unit}</b></div><div className="review-line"><span>Buying price</span><b>{formatPKR(rupeesToPaisa(buyingPrice))}</b></div><div className="review-line"><span>Selling price</span><b>{keepRate?"Keep existing":formatPKR(rupeesToPaisa(sellingPrice))}</b></div><div className="review-total"><span>Total purchase cost</span><b>{calculation?formatPKR(calculation.total):"—"}</b></div><div className="toolbar"><button type="button" className="button secondary" onClick={()=>setReview(false)}>Go back</button><button className="button" disabled={pending}>{pending?"Saving…":"Confirm and save"}</button></div></div></div>:null}</form></div>:null}</>}
export function ReceiptReversal({transactionNo,status}:{transactionNo:string;status:string}){const[state,action,pending]=useActionState(reverseReceipt,{} as ReversalState),[open,setOpen]=useState(false);if(status!=="posted")return <><button className="button secondary" type="button" onClick={()=>window.print()}>Print</button><span className="badge">Reversed</span></>;return <><button className="button secondary" type="button" onClick={()=>window.print()}>Print</button><button className="button secondary" type="button" onClick={()=>setOpen(true)}>Reverse</button>{open?<div className="review-dialog"><form action={action} className="card review-card"><input type="hidden" name="transactionNo" value={transactionNo}/><div className="section-title">Reverse {transactionNo}</div><p className="subtitle">Only safe when none of this received stock has been used or changed.</p>{state.error?<div className="form-error">{state.error}</div>:null}{state.success?<div className="form-success">{state.success}</div>:null}<div className="field"><label>Reason</label><textarea name="reason" required minLength={5}/></div><div className="toolbar"><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="button" disabled={pending}>{pending?"Reversing…":"Confirm reversal"}</button></div></form></div>:null}</>}
