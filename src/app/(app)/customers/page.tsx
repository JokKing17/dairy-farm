import{Long}from"mongodb";import{db}from"@/lib/db";import{formatPKR}from"@/lib/money";import{karachiBusinessDate}from"@/lib/queries";import{CustomerForm,PaymentForm}from"./customer-forms";export const dynamic="force-dynamic";const phone=(v:string)=>{const n=v.replace(/\D/g,"");return n.startsWith("0")?`92${n.slice(1)}`:n.startsWith("92")?n:`92${n}`};export default async function Page(){const database=await db();const rows=await database.collection("customers").aggregate([{$sort:{name:1}},{$lookup:{from:"party_ledger_entries",let:{id:"$_id"},pipeline:[{$match:{$expr:{$and:[{$eq:["$partyId","$$id"]},{$eq:["$status","posted"]}]}}},{$group:{_id:null,balance:{$sum:{$subtract:["$debitPaisa","$creditPaisa"]}}}}],as:"ledger"}},{$project:{code:1,name:1,phone:1,address:1,active:1,balance:{$ifNull:[{$first:"$ledger.balance"},0]}}}]).toArray();const today=karachiBusinessDate();return <div className="content"><div className="title">Customers</div><div className="subtitle">Daily customers and money to receive.</div><CustomerForm/><div className="card table-card table-scroll">{rows.length?<table className="table"><thead><tr><th>Code</th><th>Customer</th><th>Phone</th><th>Money to receive</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(r=>{const balance=(r.balance as Long)?.toBigInt?.()??0n;const msg=encodeURIComponent(`Assalam-o-Alaikum ${r.name}. Your DairyFlow balance on ${today} is ${formatPKR(balance)}.`);return <tr key={r._id.toString()}><td><b>{r.code}</b></td><td>{r.name}<div className="subtitle">{r.address}</div></td><td>{r.phone}</td><td>{formatPKR(balance)}</td><td><span className="badge">{r.active?"Active":"Inactive"}</span></td><td><div className="toolbar"><PaymentForm id={r._id.toString()} today={today}/><a className="button secondary" href={`https://wa.me/${phone(String(r.phone))}?text=${msg}`} target="_blank" rel="noreferrer">WhatsApp</a></div></td></tr>})}</tbody></table>:<div className="empty-state"><b>No customers yet</b><span>Add the first customer to start deliveries.</span></div>}</div></div>}
