"use server";
import { Long } from "mongodb";import { revalidatePath } from "next/cache";import { z } from "zod";import { requireSession } from "@/lib/auth";import { transaction } from "@/lib/db";import { rupeesToPaisa,quantityToMilli } from "@/lib/money";import { postPayment,paymentSchema } from "@/lib/services/payment";
export type ActionState={error?:string;success?:string};
const customer=z.object({name:z.string().trim().min(2),phone:z.string().trim().min(7).max(20),whatsapp:z.string().trim().max(20).optional(),address:z.string().trim().min(2),customerType:z.enum(["household","shop","party"]),dailyQuantity:z.string(),openingBalance:z.string().default("0")});
export async function createCustomer(_:ActionState,data:FormData):Promise<ActionState>{const actor=await requireSession(["owner","manager","accountant"]);const parsed=customer.safeParse(Object.fromEntries(data));if(!parsed.success)return{error:parsed.error.issues[0]?.message??"Check the customer details."};try{const opening=rupeesToPaisa(parsed.data.openingBalance),quantity=quantityToMilli(parsed.data.dailyQuantity);await transaction(async(db,session)=>{const now=new Date(),sequence=await db.collection("counters").findOneAndUpdate({_id:"customer" as never},{$inc:{value:1}}, {upsert:true,returnDocument:"after",session});const code=`C-${String(sequence?.value??1).padStart(4,"0")}`;const result=await db.collection("customers").insertOne({...parsed.data,code,defaultQuantityMilli:Long.fromBigInt(quantity),openingBalancePaisa:Long.fromBigInt(opening),active:true,deliveryDays:[1,2,3,4,5,6,7],createdAt:now,createdBy:actor.userId,updatedAt:now,updatedBy:actor.userId},{session});if(opening!==0n)await db.collection("party_ledger_entries").insertOne({transactionNo:`OPEN-C-${result.insertedId}`,lineNo:1,partyType:"customer",partyId:result.insertedId,businessDate:now.toISOString().slice(0,10),date:now,debitPaisa:opening>0n?Long.fromBigInt(opening):Long.ZERO,creditPaisa:opening<0n?Long.fromBigInt(-opening):Long.ZERO,description:"Opening balance",status:"posted",createdAt:now,createdBy:actor.userId},{session});await db.collection("audit_logs").insertOne({actorId:actor.userId,action:"create",entity:"customer",entityId:result.insertedId,createdAt:now},{session})});revalidatePath("/customers");return{success:"Customer added and ready for delivery."}}catch(e){return{error:e&&typeof e==="object"&&"code" in e&&e.code===11000?"Customer code already exists.":"Customer could not be saved."}}}
export async function recordCustomerPayment(_:ActionState,data:FormData):Promise<ActionState>{const actor=await requireSession(["owner","manager","accountant","cashier"]);const parsed=paymentSchema.safeParse(Object.fromEntries(data));if(!parsed.success)return{error:"Check the payment amount and method."};try{const result=await postPayment(parsed.data,actor.userId);revalidatePath("/customers");revalidatePath("/cashbook");return{success:`Receipt ${result.transactionNo}`}}catch(e){return{error:e instanceof Error?e.message:"Payment could not be recorded."}}}
